- send a json as reply to a command from UI from the model and show the content of the json in the UI.
- Explain better (add an example) in Readme.md how to use the framework
- "Rewrite" all the unittest to work with the new installation method
- Adapt controller for parameter fullscreen via constructor
- Add in MVCqtView throw exception if :/frontend doesn't exist
- Add dir examples:
	- first app demo is 4 quadrants and a number associated with each cell and when you press the number the cell change color
- Create script that generate tamplate project
- Factories and singleton classes...
- Take remote parameters...
- Rewrite UnitTest for new comunication system
- Introduci modalita' server che permette di transformare la app in web app: l'interfaccia per abilitare
  questa modalita' deve essere un cambo "server=<bool>" in app_settings.conf
- Unit test dei singoli file del nuovo controller
- Riscrivere il Makefile per il nuovo controller multifile
